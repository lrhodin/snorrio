{
  "name": "snorrio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Memory and recall infrastructure for pi sessions.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsx --test tests/**/*.test.ts",
    "install-hooks": "git config core.hooksPath .githooks"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@sinclair/typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}
